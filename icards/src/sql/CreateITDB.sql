CREATE TABLE `icardsdb`.`itequipments` (
  `id` INT NOT NULL,
  `department` VARCHAR(100) NOT NULL,
  `inventory_number` VARCHAR(10) NOT NULL,
  `name` VARCHAR(100) NOT NULL,
  `fio` VARCHAR(50) NOT NULL,
  PRIMARY KEY (`id`));
